<?xml version="1.0" encoding="utf-8"?>
<set xmlns:android="http://schemas.android.com/apk/res/android"
    android:fillAfter="true">

    <!-- Zbor în sus și în jos (translare pe axa Y) -->
    <translate
        android:fromYDelta="0%"
        android:toYDelta="-30%"
        android:duration="400"
        android:repeatCount="3"
        android:repeatMode="reverse" />

    <!-- Zbor din stânga în dreapta (translare pe axa X) -->
    <translate
        android:fromXDelta="-50%"
        android:toXDelta="50%"
        android:duration="1500" />

    <!-- Mișcare de rotație pentru a simula zborul -->
    <rotate
        android:fromDegrees="0"
        android:toDegrees="20"
        android:pivotX="50%"
        android:pivotY="50%"
        android:duration="200"
        android:repeatCount="7"
        android:repeatMode="reverse" />

    <!-- Creștere și micșorare (pulsare) -->
    <scale
        android:fromXScale="1.0"
        android:toXScale="1.2"
        android:fromYScale="1.0"
        android:toYScale="1.2"
        android:pivotX="50%"
        android:pivotY="50%"
        android:duration="300"
        android:repeatCount="5"
        android:repeatMode="reverse" />

    <!-- Apariție treptată cu efect de transparență -->
    <alpha
        android:fromAlpha="0.3"
        android:toAlpha="1.0"
        android:duration="400" />

    <!-- Dispariție treptată la final -->
    <alpha
        android:fromAlpha="1.0"
        android:toAlpha="0.0"
        android:startOffset="1300"
        android:duration="200" />
</set> 