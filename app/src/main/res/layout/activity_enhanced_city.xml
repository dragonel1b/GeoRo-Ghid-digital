<?xml version="1.0" encoding="utf-8"?>
<!-- activity_enhanced_city.xml — "Cluj Spirit" Dark Glassmorphism design
     Based on Dashboard.tsx + BentoGuide.tsx from cluj-spirit-city-guide -->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0A0F0D"
    android:fitsSystemWindows="true">

    <!-- Background ambient glow blobs (Multi-color Cluj Spirit aura) -->
    <View
        android:layout_width="320dp"
        android:layout_height="320dp"
        android:layout_gravity="top|start"
        android:layout_marginStart="-80dp"
        android:layout_marginTop="-40dp"
        android:alpha="0.6"
        android:background="@drawable/glow_blob_green" />

    <View
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_gravity="center_vertical|end"
        android:layout_marginEnd="-100dp"
        android:alpha="0.4"
        android:background="@drawable/glow_blob_blue" />

    <View
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:layout_gravity="bottom|start"
        android:layout_marginStart="-40dp"
        android:layout_marginBottom="100dp"
        android:alpha="0.3"
        android:background="@drawable/glow_blob_orange" />

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:background="@android:color/transparent"
        app:elevation="0dp">

        <com.google.android.material.appbar.CollapsingToolbarLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_scrollFlags="scroll|exitUntilCollapsed|snap">

            <!-- Animated blob image carousel -->
            <androidx.viewpager2.widget.ViewPager2
                android:id="@+id/imageCarousel"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />

            <!-- Bottom fade into dark background -->
            <View
                android:layout_width="match_parent"
                android:layout_height="140dp"
                android:layout_gravity="bottom"
                android:background="@drawable/gradient_bottom_up_dark" />

            <!-- Dot indicators -->
            <com.google.android.material.tabs.TabLayout
                android:id="@+id/imageIndicator"
                android:layout_width="match_parent"
                android:layout_height="32dp"
                android:layout_gravity="bottom"
                android:layout_marginBottom="16dp"
                android:background="@android:color/transparent"
                app:tabBackground="@drawable/tab_selector"
                app:tabGravity="center"
                app:tabIndicatorHeight="0dp"/>

            <!-- Points badge — top right, glass pill -->
            <eightbitlab.com.blurview.BlurView
                android:id="@+id/pointsBlurView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="top|end"
                android:layout_margin="16dp">
                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:paddingHorizontal="16dp"
                    android:paddingVertical="10dp"
                    android:background="@drawable/pill_glass_orange_border">
                    <!-- Gold star -->
                    <TextView
                        android:layout_width="18dp"
                        android:layout_height="18dp"
                        android:text="★"
                        android:textSize="12sp"
                        android:textColor="#EAB308"
                        android:textStyle="bold"
                        android:gravity="center" />
                    <TextView
                        android:id="@+id/pointsTextView"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="8dp"
                        android:text="Puncte: 0"
                        android:textSize="13sp"
                        android:textStyle="bold"
                        android:textColor="#FFFFFF"/>
                </LinearLayout>
            </eightbitlab.com.blurview.BlurView>

        </com.google.android.material.appbar.CollapsingToolbarLayout>
    </com.google.android.material.appbar.AppBarLayout>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingBottom="100dp"
        app:behavior_overlapTop="0dp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <LinearLayout
            android:id="@+id/cityContentContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingTop="16dp"
            android:paddingHorizontal="16dp"
            android:paddingBottom="24dp">

            <!-- All dynamic content (sections, attractions, widgets) injected here -->

            <Space
                android:layout_width="match_parent"
                android:layout_height="24dp" />

            <!-- CTA Button — matches FAB-like orange button from React -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/viewCitiesButton"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:layout_marginBottom="8dp"
                android:text="@string/view_cities"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="#FFFFFF"
                app:cornerRadius="20dp"
                app:backgroundTint="#F97316"
                app:icon="@drawable/ic_city"
                app:iconTint="#FFFFFF"
                app:iconPadding="12dp"
                app:iconGravity="textStart"/>

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

    <!-- FAB — matches the orange rounded-xl in React Dashboard -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="24dp"
        android:contentDescription="Info"
        app:srcCompat="@drawable/ic_info"
        app:tint="#FFFFFF"
        app:backgroundTint="#F97316"
        app:elevation="12dp"
        app:shapeAppearanceOverlay="@style/FABRounded"
        app:fabSize="normal" />

    <nl.dionsegijn.konfetti.xml.KonfettiView
        android:id="@+id/confetti_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>