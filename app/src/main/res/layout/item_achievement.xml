<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <ImageView
            android:id="@+id/achievementIcon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:contentDescription="@string/achievement_icon_description"
            tools:src="@drawable/ic_trophy_new" />

        <TextView
            android:id="@+id/achievementTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:textStyle="bold"
            android:textSize="16sp"
            app:layout_constraintStart_toEndOf="@id/achievementIcon"
            app:layout_constraintTop_toTopOf="@id/achievementIcon"
            app:layout_constraintEnd_toStartOf="@id/achievementUnlockedBadge"
            tools:text="Achievement Title" />

        <TextView
            android:id="@+id/achievementDescription"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            app:layout_constraintStart_toStartOf="@id/achievementTitle"
            app:layout_constraintTop_toBottomOf="@id/achievementTitle"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="Complete description of the achievement and what the user needs to do to unlock it." />

        <ProgressBar
            android:id="@+id/achievementProgressBar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            app:layout_constraintStart_toStartOf="@id/achievementDescription"
            app:layout_constraintTop_toBottomOf="@id/achievementDescription"
            app:layout_constraintEnd_toStartOf="@id/achievementProgressText"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:progress="75" />

        <TextView
            android:id="@+id/achievementProgressText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            app:layout_constraintStart_toEndOf="@id/achievementProgressBar"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="@id/achievementProgressBar"
            app:layout_constraintTop_toTopOf="@id/achievementProgressBar"
            tools:text="75%" />

        <ImageView
            android:id="@+id/achievementUnlockedBadge"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@android:drawable/btn_star_big_on"
            android:visibility="gone"
            android:contentDescription="@string/achievement_unlocked_description"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/achievementTitle"
            tools:visibility="visible" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView> 