<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    style="@style/RomanianBottomSheet">

    <!-- Header with Close Button -->
    <LinearLayout
        style="@style/RomanianContainer.Header"
        android:paddingTop="8dp"
        android:paddingBottom="16dp">

        <TextView
            android:id="@+id/cityName"
            style="@style/RomanianText.Title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"/>

        <ImageButton
            android:id="@+id/closeButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_close"
            android:contentDescription="Close button"
            app:tint="@color/rom_text_primary"/>
    </LinearLayout>

    <!-- Image Carousel -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/imageCarousel"
        style="@style/RomanianCarousel"
        android:layout_marginBottom="8dp"/>

    <!-- Image Indicator -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/imageIndicator"
        style="@style/RomanianTabLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"/>

    <!-- City Description -->
    <TextView
        android:id="@+id/cityDescription"
        style="@style/RomanianText.Description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

</LinearLayout>
