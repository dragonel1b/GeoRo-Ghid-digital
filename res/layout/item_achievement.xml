<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp">

        <!-- Achievement Icon -->
        <ImageView
            android:id="@+id/achievementIcon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/circle_background"
            android:padding="10dp"
            android:src="@drawable/ic_trophy"
            app:tint="@color/rom_primary" />

        <!-- Content -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginStart="16dp">

            <!-- Title and Badge -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/achievementTitle"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Titlu Realizare"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:textColor="@color/rom_text_primary"/>

                <ImageView
                    android:id="@+id/achievementUnlockedBadge"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:src="@drawable/ic_check"
                    android:visibility="gone"
                    app:tint="@color/rom_success"/>
            </LinearLayout>

            <!-- Description -->
            <TextView
                android:id="@+id/achievementDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Descriere realizare cu detalii despre cum să o deblochezi"
                android:textSize="14sp"
                android:textColor="@color/rom_text_secondary"
                android:layout_marginTop="4dp"/>

            <!-- Progress -->
            <TextView
                android:id="@+id/achievementProgress"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Progres: 0/10"
                android:textSize="12sp"
                android:textColor="@color/rom_text_hint"
                android:layout_marginTop="8dp"/>

            <ProgressBar
                android:id="@+id/achievementProgressBar"
                style="@style/Widget.AppCompat.ProgressBar.Horizontal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:progress="50"
                android:layout_marginTop="4dp"
                android:progressTint="@color/rom_primary"/>
        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView> 