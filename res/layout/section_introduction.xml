<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@color/rom_background_light">

    <!-- Section Title -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/dobrogea_intro_title"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/rom_text_primary"
        android:layout_marginBottom="8dp"/>

    <!-- Section Content -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/dobrogea_intro_content"
        android:textSize="16sp"
        android:textColor="@color/rom_text_secondary"/>

    <!-- Image Carousel Placeholder -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/imageCarousel"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_marginTop="16dp"
        android:clipToPadding="false"
        android:paddingHorizontal="32dp"/>
</LinearLayout>
